# Deployment settings
MDB_CONTAINER_NAME=marple-db # Unique name on server
TRINO_CONTAINER_NAME=trino # Unique name on server
MDB_VERSION=3.8.0
MDB_DEPLOYMENT="TODO, ask Marple team"

# Persistent storage, where files and logs will be stored
DOCKER_PATH_ROOT=/srv/marple/db

# Leave like this if you use postgres in docker
# Otherwise, set credentials of postgres server
MDB_DB_HOST=marpledb-postgres
MDB_DB_USER=marple
MDB_DB_PASSWORD=postgresindocker
MDB_DB_NAME=marpledb
MDB_DB_PORT=5432

# Trino
MDB_TRINO_HOST="TODO, e.g. query.db.{customer}.marpledata.com"
MDB_TRINO_PORT=443

# Web server
MDB_PORT=8000
TRINO_PORT=8080

TRINO_SHARED_SECRET="TODO"

# Sane defaults for 8GB VM => 4GB Trino
TRINO_QUERY_MAX_MEMORY="1.5GB"
TRINO_QUERY_MAX_MEMORY_PER_NODE="1.5GB"
TRINO_HEAP_HEADROOM_PER_NODE="1GB"
TRINO_QUERY_MAX_TOTAL_MEMORY="3GB"


# Container resource limits (CPU cores / memory)
# Unset (the default) means unlimited. Uncomment to cap a container.
# NOTE: TRINO_MEM_LIMIT must exceed TRINO_QUERY_MAX_MEMORY_PER_NODE + TRINO_HEAP_HEADROOM_PER_NODE
# plus JVM overhead, otherwise Trino fails to start. Suggested values for an 8GB VM:
# MDB_CPU_LIMIT=2
# MDB_MEM_LIMIT=2g
# MDB_WORKER_CPU_LIMIT=2
# MDB_WORKER_MEM_LIMIT=2g
# TRINO_CPU_LIMIT=2.5
# TRINO_MEM_LIMIT=4g

